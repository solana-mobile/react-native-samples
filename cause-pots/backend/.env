# Server Configuration
PORT=3000

# CORS Configuration
# ⚠️ SECURITY WARNING: CORS is set to '*' (allow all origins) for demo/development only.
# For production, replace with specific allowed origins:
# CORS_ORIGIN=https://your-production-app.com
CORS_ORIGIN=*

# Database Configuration
# Path to SQLite database file (relative to backend directory)
# DB_PATH=./data/cause-pots.db
